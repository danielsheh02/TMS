import axios from "axios";

const API_URL = process.env.REACT_APP_API_URL;


export default class ProjectSelectionService {

    // static getProjects() {
    //     return axios.get(API_URL + "api/v1/suites/", {headers: authHeader()})
    // }
    //
    // static createProject(name: string, description: string) {
    //     axios.post(API_URL + "api/v1/suites/", suite, {headers: authHeader()})
    //         .then(() => {})
    // }
}
